# Qwen WebUI 仅保留文本输入

## 需求

成都徒步 ChatBI 的 WebUI 仅接受文本问题，不展示文件上传按钮和麦克风录音组件。

## 行为

- `MultimodalInput` 关闭文件上传能力。
- 不创建 Gradio `Audio` 组件。
- 提交事件在音频关闭时只处理文本输入。
- Qwen Agent 通用 WebUI 默认行为保持兼容；只有业务侧明确关闭时才隐藏组件。

## 验收

- 页面不再出现“点击上传”。
- 页面不再出现“音频”或“录制”。
- 文本输入、提交和推荐对话仍然可用。

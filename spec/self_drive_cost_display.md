# 自驾路线费用展示

## 需求

`routes.cost_min_cny` 和 `routes.cost_max_cny` 表示预计报团费用区间。

用户选定自驾作为交通方式后，最终方案不得展示这两个字段对应的“路线费用”或报团费用，也不展示“预计总费用”。

当工具结果表明自驾交通没有额外费用、油费由用户自理时，应展示：

- 自驾交通费：油费自理，无额外交通费用

底层工具返回的费用明细保持不变，本规则仅约束面向用户的回答文案。

# 移动端 H5 快捷提问底部布局

## 需求

- H5 聊天框应占用标题与输入区域之间的剩余高度，初始化时显示得更长。
- 输入框和“快捷提问”作为一个整体位于页面底部安全区域上方。
- 小屏设备中聊天框允许收缩，输入框和快捷提问不得被挤出视口。

## 验收

- H5 主容器采用三行网格布局：标题、可伸缩聊天框、底部输入区域。
- 聊天框所在行使用 `minmax(0, 1fr)` 消化剩余高度。
- 底部输入区域不参与拉伸。


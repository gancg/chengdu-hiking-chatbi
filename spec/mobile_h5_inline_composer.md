# 移动端 H5 单行输入区

## 需求

- H5 用户输入框与提交按钮在同一行展示，减少输入区域的纵向占用。
- 输入框使用剩余宽度，提交按钮保持稳定宽度。
- 输入内容较长时仍允许输入框内部换行，不得横向撑破页面。
- 快捷提问继续位于输入区域下方。

## 验收

- H5 输入组件具有独立样式类 `.h5-input`。
- 输入组件采用 `minmax(0, 1fr) auto` 两列网格。
- 按钮控制区清除默认上边距并与输入框垂直居中。
- 空的工具按钮区域不占用横向空间。

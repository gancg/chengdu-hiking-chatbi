# 暂停公共交通出行方式

## 需求

自然语言模型暂时不提供公共交通出行方案，避免整合并展示当前质量不稳定的公共交通信息。

## 规则

- 路线选定后的交通方式仅展示“自驾”和“报团”。
- 模型不得主动推荐、询问或选择公共交通。
- 模型调用推荐工具时若传入 `public_transit`，工具应抛出含义明确的异常。
- 底层路线数据、数据库字段和公共交通计算能力暂时保留，不做删除，方便后续恢复和独立测试。

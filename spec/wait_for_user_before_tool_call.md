# 提问后等待用户回答

## 规则

模型向用户提出需要回答的问题时，当前 Agent 运行必须结束并等待新的用户消息。

如果模型在同一批输出中既生成了提问文本，又生成了工具调用，执行层应保留提问文本，但不得展示或执行该批工具调用。不得把“如果没有要求就直接推荐”等假设视为用户已经回答。

“已提问”状态必须覆盖整轮流式生成。即使提问文本与工具调用位于不同流片段，后续工具调用也不得执行。

普通的工具调用说明（例如“我来查询日期”）不属于等待用户回答的问题，可以继续执行工具。
